pipeline {
    agent any

    stages {
        stage('Docker') {
            steps {
                sh "sudo docker build -t jilu407/aravind-app:$BUILD_NUMBER ."
            }
        }
        stage('Docker clean') {
            steps {
                sh "docker system prune -af --volumes"
            }
        }
    }
}
